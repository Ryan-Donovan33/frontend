const initialState = {
  gigaPet: {
    nickname: "MrHatBirb",
    health: 75
  }
};

const petReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default petReducer;
