const initialState = {
  gigaPet: {
    nickname: "MrHatBirb",
    health: 150
  }
};

const petReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default petReducer;
